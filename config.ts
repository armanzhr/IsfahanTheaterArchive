const config = {
  auth: {
    enabled: false,
  },
  payments: {
    enabled: true,
  },
  features: {
    blog: true,
  },
  baseURL: "https://api.isfahantheaters.ir/api",
  fileURL: "https://api.isfahantheaters.ir/",
};

export default config;
