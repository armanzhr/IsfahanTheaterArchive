const config = {
  auth: {
    enabled: false,
  },
  payments: {
    enabled: true,
  },
  features: {
    blog: true,
  },
  baseURL: "http://api.isfahantheaters.ir/api",
  fileURL: "http://api.isfahantheaters.ir/",
};

export default config;
